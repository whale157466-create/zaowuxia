# Impeccable UI Polish 规则【本手工电商Web项目专用】
> 仅用于页面视觉美化，严禁擅自改动业务与页面结构
  

## 🚨最高强制约束（优先级最高，必须严格遵守）
1. DOM结构完全锁死：**禁止新增、删除、移动、重排任何HTML标签、组件顺序**。不许修改标签层级。
2. JavaScript全部不动：业务逻辑、接口请求、事件、变量、条件判断，一律不修改。
3. 只允许修改内容：CSS / Tailwind类名，仅做视觉样式调整。


## UI设计规范（参考Impeccable）

排版：
- 采用模块化字体比例，比例≥1.25
- 正文行宽控制 65‑75ch，对齐基线网格
- 禁止无脑使用 Inter 字体

色彩：
- 使用 OKLCH 色彩空间，中性色带上品牌染色
- 禁止使用纯黑 `#000`、纯白 `#fff`
- 深色模式降低色彩饱和度
- 严格满足 WCAG‑AA 无障碍对比度
- 禁止紫蓝渐变、霓虹高亮、渐变文字

布局与间距：
- 基于 8px 间距 token，禁止硬编码固定数值
- 制造视觉节奏，避免全部 padding 完全一致
- 禁止卡片嵌套、所有内容套卡片、全部强制居中，优先左对齐
- 合理使用容器查询 Container Query

动效：
- 仅对 `transform`、`opacity` 做动画
- 使用 `ease‑out` 系列缓动
- **禁用 bounce / elastic 弹跳类动画**
- 尊重系统“减少动效”设置，只保留功能性动效，去掉纯装饰抖动动画
- 禁止动画 width / height / padding / margin

交互 & 响应式：
- 完善 hover / focus / disabled / error / 空状态 / 加载状态
- 键盘可访问，焦点状态可见
- 移动优先，合理使用 `clamp()`，不要滥用流式字号

黑名单（严格禁止）：
- 卡片嵌套卡片、千篇一律网格卡片列表
- 标题搭配大圆角图标模板样式
- 彩色渐变装饰元素
- 动画操作 width、height、padding、margin

> 本项目为产品工具类页面，可用性优先，拒绝过度花哨视觉
> 可执行命令：`/polish`像素对齐打磨｜`/arrange`修复布局间距｜`/audit`无障碍自检

## 输出约定
1. 只输出被修改的代码片段，标记出改动的部分，不要输出完整大文件。
2. 不要输出Figma相关指令，不要输出图片生成指令。
3. 我会在浏览器查看渲染后的页面效果，再手动复刻到Figma原型。