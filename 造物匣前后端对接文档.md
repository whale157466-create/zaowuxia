# 造物匣 — 前后端对接文档

> 版本: v1.0 | 前端技术栈: Vue 3 + Vite + Element Plus + Pinia + TypeScript | 日期: 2026-07-24

---

## 一、接口文档

### 1.1 统一返回格式

所有接口统一返回以下 JSON 结构：

```json
{
  "code": 0,           // 0=成功, 非0=失败 (错误码见附录)
  "message": "ok",     // 提示信息
  "data": {}           // 业务数据
}
```

分页接口 data 结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "pageSize": 10
  }
}
```

### 1.2 鉴权方式

- 用户端请求头: `Authorization: Bearer {token}`
- 管理端请求头: `Authorization: Bearer {admin_token}`
- Token 由登录接口返回，前端存入 `localStorage`，每次请求通过 axios 拦截器自动携带
- Token 过期时后端返回 `code: 401`，前端自动跳转登录页

### 1.3 用户端接口清单

#### 1.3.1 微信扫码登录

```
POST /api/user/login/wechat
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| code | string | 是 | 微信 OAuth 回调 code |

**返回：**

```json
{ "code": 0, "message": "ok", "data": { "token": "eyJhbG..." } }
```

#### 1.3.2 账号密码登录

```
POST /api/user/login/password
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| account | string | 是 | 邮箱或手机号 |
| password | string | 是 | 密码 |

**返回：** 同 1.3.1

#### 1.3.3 获取分类列表

```
GET /api/categories
```

**返回：**

```json
{
  "code": 0,
  "data": [
    { "id": "c1", "name": "微缩蛋糕", "icon": "🍰" },
    { "id": "c2", "name": "篆刻入门", "icon": "🔖" }
  ]
}
```

#### 1.3.4 商品列表（搜索 + 筛选 + 分页）

```
GET /api/products
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | string | 否 | 搜索关键词 |
| categoryId | string | 否 | 分类ID |
| difficulty | string | 否 | 难度: beginner / intermediate / advanced |
| sort | string | 否 | price_asc / price_desc |
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页数量，默认10 |

**返回：**

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": "p1",
        "name": "微缩蛋糕·草莓奶油杯",
        "categoryId": "c1",
        "categoryName": "微缩蛋糕",
        "difficulty": "beginner",
        "images": ["https://cdn.example.com/img1.jpg"],
        "description": "<h3>材料清单</h3>...",
        "skus": [
          { "id": "s1a", "name": "基础套装", "price": 39.9, "stock": 50, "image": "" }
        ],
        "status": "on",
        "createdAt": "2026-07-01T00:00:00Z"
      }
    ],
    "total": 42,
    "page": 1,
    "pageSize": 10
  }
}
```

#### 1.3.5 商品详情

```
GET /api/products/:id
```

**返回：** 单个 Product 对象（结构同 1.3.4 list 项）

#### 1.3.6 一键配齐物料清单

```
GET /api/products/:id/kit
```

**返回：**

```json
{
  "code": 0,
  "data": [
    {
      "productId": "p_mat1",
      "productName": "超轻粘土（3色套装）",
      "skuId": "sk_mat1",
      "skuName": "标准装",
      "price": 15.00,
      "stock": 100,
      "quantity": 1,
      "type": "material",
      "checked": true
    }
  ]
}
```

#### 1.3.7 获取购物车

```
GET /api/cart
```

**返回：** CartItem 数组。金额计算由后端完成，前端仅展示。

```json
{
  "code": 0,
  "data": [
    {
      "id": "c1",
      "productId": "p1",
      "productName": "微缩蛋糕·草莓奶油杯",
      "productImage": "https://cdn.example.com/img1.jpg",
      "skuId": "s1a",
      "skuName": "基础套装",
      "price": 39.90,
      "quantity": 2,
      "stock": 50,
      "checked": true,
      "invalid": false
    }
  ]
}
```

#### 1.3.8 加入购物车

```
POST /api/cart
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| skuId | string | 是 | SKU ID |
| quantity | number | 是 | 数量 |

**后端逻辑要求：**
- 校验库存是否充足，不足则返回错误
- 已存在同 SKU 时合并数量（而非新增条目）
- 返回更新后的购物车列表

#### 1.3.9 更新购物车商品

```
PUT /api/cart/:id
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| quantity | number | 否 | 新数量 |
| checked | boolean | 否 | 是否勾选 |

#### 1.3.10 删除购物车商品

```
DELETE /api/cart/:id
```

#### 1.3.11 批量加入购物车（一键配齐）

```
POST /api/cart/batch
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| items | Array<{skuId, quantity}> | 是 | 批量商品列表 |

#### 1.3.12 收货地址列表

```
GET /api/addresses
```

**返回：**

```json
{
  "code": 0,
  "data": [
    {
      "id": "a1",
      "recipient": "张三",
      "phone": "13800138000",
      "province": "广东省",
      "city": "深圳市",
      "district": "南山区",
      "detail": "科技园路1号",
      "isDefault": true
    }
  ]
}
```

#### 1.3.13 新增 / 编辑地址

```
POST   /api/addresses          # 新增
PUT    /api/addresses/:id       # 编辑
DELETE /api/addresses/:id       # 删除
```

#### 1.3.14 优惠券列表

```
GET /api/coupons?amount=128.50
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| amount | number | 是 | 当前订单商品总额，用于过滤可用券 |

**返回：**

```json
{
  "code": 0,
  "data": [
    {
      "id": "cp1",
      "name": "新人专享券",
      "discount": 10.00,
      "minAmount": 50.00,
      "expireAt": "2026-12-31T23:59:59Z",
      "used": false
    }
  ]
}
```

#### 1.3.15 提交订单

```
POST /api/orders
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| items | Array<{skuId, quantity}> | 是 | 商品列表 |
| addressId | string | 是 | 收货地址ID |
| couponId | string | 否 | 优惠券ID |
| remark | string | 否 | 订单备注 |

**后端逻辑要求：**
- **核心：金额由后端计算**（商品价格、优惠抵扣、运费规则均以后端为准）
- 校验库存：任一商品库存不足则拒绝下单
- 校验优惠券：是否过期、是否满足门槛
- 校验地址：是否为有效地址、是否在配送范围
- 返回订单 ID、订单号、实付金额

**返回：**

```json
{
  "code": 0,
  "data": {
    "orderId": "o_new",
    "orderNo": "202607240005",
    "totalAmount": 168.80
  }
}
```

#### 1.3.16 发起支付

```
POST /api/orders/:id/pay
```

**返回：**

```json
{
  "code": 0,
  "data": {
    "qrCodeUrl": "weixin://wxpay/bizpayurl?pr=...",
    "h5Url": "https://wx.tenpay.com/..."
  }
}
```

**后端逻辑：**
- 生成微信支付预支付订单
- Web 端返回二维码链接，前端展示扫码支付
- 前端轮询或 WebSocket 获取支付结果

#### 1.3.17 订单列表

```
GET /api/orders?status=pending_payment
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | string | 否 | pending_payment / pending_ship / pending_receive / completed / after_sales |

**返回：** Order 数组

#### 1.3.18 订单详情

```
GET /api/orders/:id
```

#### 1.3.19 取消订单

```
PUT /api/orders/:id/cancel
```

**后端逻辑：** 仅待付款状态可取消，已支付订单走售后流程。

#### 1.3.20 确认收货

```
PUT /api/orders/:id/receive
```

**后端逻辑：** 仅待收货状态可确认，确认后状态变更为已完成。

#### 1.3.21 提交售后申请

```
POST /api/after-sales
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| orderId | string | 是 | 订单ID |
| type | string | 是 | refund / return / reissue |
| reason | string | 是 | 申请原因 |
| images | string[] | 否 | 凭证图片 base64 或 URL 数组 |

### 1.4 管理端接口

#### 1.4.1 管理员登录

```
POST /api/admin/login
```

| 参数 | 类型 | 必填 |
|------|------|------|
| account | string | 是 |
| password | string | 是 |

#### 1.4.2 管理端商品管理

```
GET    /api/admin/products           # 列表
PUT    /api/admin/products/:id       # 更新（上架/下架/库存/售价/分类）
POST   /api/admin/products/batch     # 批量操作 { ids, action }
```

#### 1.4.3 管理端订单管理

```
GET    /api/admin/orders             # 列表
POST   /api/admin/orders/:id/ship    # 发货 { company, trackingNo }
POST   /api/admin/after-sales/:id/review  # 售后审核 { approved, reason }
```

---

## 二、实体字段清单

### 2.1 用户表 (users)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | varchar(36) | 是 | 主键 UUID |
| open_id | varchar(64) | 否 | 微信 OpenID |
| nickname | varchar(50) | 是 | 昵称 |
| avatar | varchar(500) | 否 | 头像URL |
| phone | varchar(20) | 否 | 手机号 |
| email | varchar(100) | 否 | 邮箱 |
| created_at | datetime | 是 | 注册时间 |

### 2.2 商品表 (products)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | varchar(36) | 是 | 主键 |
| name | varchar(200) | 是 | 商品名称 |
| category_id | varchar(36) | 是 | 分类ID |
| category_name | varchar(100) | 是 | 分类名称（冗余，避免JOIN） |
| difficulty | enum('beginner','intermediate','advanced') | 是 | 难度 |
| images | json | 是 | 图片URL数组 |
| description | text | 是 | 富文本描述 |
| status | enum('on','off') | 是 | 上架/下架 |
| created_at | datetime | 是 | 创建时间 |

### 2.3 SKU 表 (skus)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | varchar(36) | 是 | 主键 |
| product_id | varchar(36) | 是 | 商品ID |
| name | varchar(100) | 是 | 规格名称 |
| price | decimal(10,2) | 是 | **核心金额字段，以后端为准** |
| stock | int | 是 | **核心库存字段，以后端为准** |
| image | varchar(500) | 否 | 规格图 |

### 2.4 购物车表 (cart_items)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | varchar(36) | 是 | 主键 |
| user_id | varchar(36) | 是 | 用户ID |
| sku_id | varchar(36) | 是 | SKU ID |
| quantity | int | 是 | 数量 |
| checked | boolean | 是 | 是否勾选，默认 true |
| created_at | datetime | 是 | 加入时间 |

### 2.5 订单表 (orders)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | varchar(36) | 是 | 主键 |
| order_no | varchar(20) | 是 | 订单号 |
| user_id | varchar(36) | 是 | 用户ID |
| items | json | 是 | 订单商品快照 |
| address_snapshot | json | 是 | 地址快照 |
| coupon_snapshot | json | 否 | 优惠券快照 |
| product_amount | decimal(10,2) | 是 | 商品总额 |
| discount_amount | decimal(10,2) | 是 | 优惠金额 |
| freight | decimal(10,2) | 是 | 运费 |
| total_amount | decimal(10,2) | 是 | 实付金额 |
| status | enum | 是 | 订单状态 |
| remark | varchar(500) | 否 | 备注 |
| expire_at | datetime | 否 | 支付截止时间 |
| created_at | datetime | 是 | 下单时间 |

### 2.6 其他辅助表

- **addresses**: id, user_id, recipient, phone, province, city, district, detail, is_default
- **coupons**: id, name, discount, min_amount, expire_at, total_count, used_count
- **user_coupons**: id, user_id, coupon_id, used
- **after_sales**: id, order_id, user_id, type, reason, images, refund_amount, status, reject_reason, created_at
- **logistics**: order_id, company, tracking_no, traces (json)

---

## 三、数据规范约定

### 3.1 Pinia 全局数据结构

**userStore：**

```ts
{
  user: User | null,
  token: string | null,
  loading: boolean,
  isLoggedIn: boolean  // computed
}
```

**cartStore：**

```ts
{
  items: CartItem[],
  loading: boolean,
  validItems: CartItem[],     // computed, 过滤失效
  checkedItems: CartItem[],   // computed, 已勾选有效商品
  totalCount: number,         // computed, 已选总件数
  totalAmount: number         // computed, 已选总金额（仅展示用，结算以后端为准）
}
```

### 3.2 Token 传递方式

```
Authorization: Bearer {token}
```

- 登录成功后前端写入 `localStorage.setItem('token', token)`
- axios 请求拦截器自动附加到每个请求头
- 后端返回 401 时前端清除 token 并跳转登录页

### 3.3 时间格式标准

- 所有时间字段以 **ISO 8601** 字符串传输: `"2026-07-24T10:30:00+08:00"` 或 `"2026-07-24 10:30:00"`
- 前端使用 `dayjs` 库格式化展示
- 优惠券有效期、订单过期时间等以服务端时间为准

### 3.4 金额标准

- **所有金额以后端计算和返回为准**
- 传输格式: `decimal(10,2)` → JSON `number`，如 `39.90`
- 前端不做金额计算，仅做展示格式化 `¥{value.toFixed(1)}`
- 涉及计算: 商品总价、优惠抵扣、运费、实付金额均由后端完成

### 3.5 状态编码标准

**订单状态 (OrderStatus)：**

| 编码 | 含义 |
|------|------|
| pending_payment | 待付款 |
| pending_ship | 待发货 |
| pending_receive | 待收货 |
| completed | 已完成 |
| cancelled | 已取消 |

**商品难度 (Difficulty)：**

| 编码 | 含义 |
|------|------|
| beginner | 入门 |
| intermediate | 进阶 |
| advanced | 高阶 |

**售后类型 (AfterSalesType)：**

| 编码 | 含义 |
|------|------|
| refund | 退款 |
| return | 退货 |
| reissue | 缺件补发 |

**售后状态 (AfterSalesStatus)：**

| 编码 | 含义 |
|------|------|
| reviewing | 审核中 |
| approved | 已通过 |
| processing | 处理中 |
| completed | 已完成 |
| rejected | 已拒绝 |

### 3.6 统一错误码

| code | 含义 | 前端处理 |
|------|------|---------|
| 0 | 成功 | 正常展示 |
| 401 | Token 过期/无效 | 跳转登录页 |
| 403 | 权限不足 | Toast 提示 |
| 400 | 参数校验失败 | 展示具体 message |
| 409 | 库存不足 | Toast "该商品库存不足" |
| 429 | 风控限流 | Toast "操作频繁，请稍后再试" |
| 500 | 服务端异常 | 展示错误兜底页 |

---

## 四、业务规则文档

### 4.1 库存校验

- **加入购物车时**: 前端轻量校验 `quantity <= stock`，后端做最终校验
- **结算时**: 后端逐个校验每个勾选商品的库存，任一不足则拒绝整单，返回不足商品列表
- **支付时**: 不扣减库存，支付成功后扣减（或预占库存机制由后端决定）
- **超时未支付**: 订单自动取消，库存释放

### 4.2 购物车合并

- 同一用户同一 SKU 重复加入购物车 → 数量叠加而非新增条目
- 购物车数据以服务端为准，前端每次操作后重新拉取
- 登录前后购物车合并由后端处理（本地购物车 → 服务端合并）

### 4.3 金额计算

- **前端职责**: 仅做展示，所有金额以后端返回为准
- **后端计算公式**:
  - `productAmount` = Σ(sku.price × quantity)
  - `discountAmount` = 优惠券抵扣金额（后端校验有效期和门槛）
  - `freight` = 运费规则（如满 99 免运费）
  - `totalAmount` = productAmount - discountAmount + freight（≥ 0）

### 4.4 订单状态流转

```
下单 → pending_payment → (支付成功) → pending_ship → (发货) → pending_receive → (确认收货) → completed
         ↓ 超时/取消
      cancelled
```

### 4.5 售后规则

- 仅 `completed` 状态可申请售后
- 售后进行中的订单不可确认收货 (E042)
- 售后期限、不支持售后的商品类型由后端配置
- 退款原路返回，账户注销情况需人工处理 (E041)

---

## 五、前端项目说明

### 5.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.5 | 渐进式框架 |
| Vite | ^6.0 | 构建工具 |
| Element Plus | ^2.9 | UI 组件库 |
| Pinia | ^2.2 | 状态管理 |
| Vue Router | ^4.5 | 路由 |
| TypeScript | ~5.6 | 类型安全 |
| Axios | ^1.7 | HTTP 请求 |
| dayjs | ^1.11 | 日期格式化 |

### 5.2 环境变量

在项目根目录创建 `.env.development`:

```env
VITE_API_BASE_URL=http://localhost:8080/api
VITE_USE_MOCK=true
```

### 5.3 Mock 切换真实接口

1. 打开 `src/api/index.ts`
2. 找到 `const USE_MOCK = true`
3. 改为 `const USE_MOCK = false`
4. 确保 `.env` 中 `VITE_API_BASE_URL` 指向真实后端地址
5. 确认后端接口路径与 `api/index.ts` 中定义一致

### 5.4 源码目录说明

```
zaowuxia-vue/
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── src/
│   ├── main.ts                    # 入口：挂载 Vue + Pinia + Router + Element Plus
│   ├── App.vue                    # 根组件
│   ├── env.d.ts                   # 类型声明
│   ├── assets/
│   │   └── style.css              # 全局样式 + CSS 变量（色调）
│   ├── types/
│   │   └── index.ts               # 所有 TS 类型定义
│   ├── api/
│   │   └── index.ts               # API 层：Mock 数据 + 真实接口（切换 USE_MOCK）
│   ├── stores/
│   │   ├── user.ts                # Pinia 用户状态
│   │   └── cart.ts                # Pinia 购物车状态
│   ├── router/
│   │   └── index.ts               # 路由配置（懒加载）
│   ├── components/
│   │   ├── common/
│   │   │   ├── EmptyState.vue     # 空状态组件（E010~E018）
│   │   │   └── NotFound.vue       # 404 页面
│   │   └── layout/
│   │       ├── UserLayout.vue     # 用户端布局（顶部导航 + 内容 + 底部）
│   │       └── AdminLayout.vue    # 管理端布局（侧边栏 + 内容）
│   └── views/
│       ├── user/                   # 用户端页面
│       │   ├── home/Index.vue      # 首页（项目分类）
│       │   ├── login/Index.vue     # 登录页（微信扫码 + 账号密码 + 游客）
│       │   ├── product/List.vue    # 商品列表与搜索
│       │   ├── product/Detail.vue  # 商品详情
│       │   ├── kit/Index.vue       # 一键配齐
│       │   ├── cart/Index.vue      # 购物车
│       │   ├── checkout/Index.vue  # 下单结算
│       │   ├── payment/Index.vue   # 支付页
│       │   ├── orders/Index.vue    # 订单列表
│       │   ├── orders/Detail.vue   # 订单详情
│       │   ├── afterSales/Index.vue # 售后申请
│       │   └── profile/            # 个人中心
│       │       ├── Index.vue       # 个人中心主页
│       │       ├── Addresses.vue   # 收货地址管理
│       │       ├── Favorites.vue   # 我的收藏
│       │       └── History.vue     # 浏览记录
│       └── admin/                  # 管理端页面
│           ├── login/Index.vue      # 管理端登录
│           ├── products/Index.vue   # 商品管理
│           └── orders/Index.vue     # 订单管理
```

### 5.5 启动与构建

```bash
# 安装依赖
npm install

# 开发模式
npm run dev          # → http://localhost:5173

# 生产构建
npm run build        # → dist/
npm run preview      # 预览构建产物
```

---

## 附录：页面 ↔ 模块编号对照表

| 页面 | 模块编号 | 弹窗 |
|------|---------|------|
| 登录页 | M1-1~M1-5 | D1, D2 |
| 首页 | M2-1~M2-4 | - |
| 商品列表 | M3-1~M3-5 | D5 |
| 商品详情 | M4-1~M4-7 | D3, D4 |
| 一键配齐 | M5-1~M5-6 | - |
| 购物车 | M6-1~M6-6 | D18 |
| 下单结算 | M7-1~M7-6 | D6, D7 |
| 支付页 | M8-1~M8-5 | D8 |
| 订单列表 | M9-1~M9-3 | D9, D11 |
| 订单详情 | M9-4~M9-7 | D10, D12 |
| 售后申请 | M10-1~M10-7 | - |
| 个人中心 | M11-1~M11-3 | D13, D14, D16 |
| 全局 | G-1~G-5 | D17 |
| 管理端登录 | AM1-1~AM1-2 | - |
| 商品管理 | AM2-1~AM2-9 | D19~D22 |
| 订单管理 | AM3-1~AM3-7 | D23~D25 |

异常状态 E001~E055 已全部实现在各页面的空状态、错误处理、表单校验逻辑中。
